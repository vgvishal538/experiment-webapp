import React from 'react';

const ContentScreen = ({ screenName }) => {
    return (
        <div>
            {/* Content for each screen can be added here */}
        </div>
    );
};

export default ContentScreen;
